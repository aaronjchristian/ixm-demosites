/**
 * @file
 * Simple helpers for the swat4 theme.
 */

(function ($, Drupal, drupalSettings) {

  'use strict';

  /**
   * Core/Misc behaviours.
   */
  Drupal.behaviors.swat4Misc = {
    attach: function (context, settings) {
      console.log('here');
    }
  };

})(jQuery, Drupal, drupalSettings);
